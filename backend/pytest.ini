[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    live_gemini: Live external Gemini integration tests (requires real GEMINI_API_KEY)
addopts = -m "not live_gemini"
